# Historical implementation records

This directory keeps earlier plans, handoffs, and the old agent journal for
traceability. Their phase names, test counts, provider status, and stop points
describe past snapshots and may no longer match the current application.

For the current product overview, read the repository root `README.md`. For
current code behavior, use the source and tests. Do not treat an archived plan
as a feature that is necessarily shipped.
